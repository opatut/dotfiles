alias spacman='sudo pacman'
alias sc='sudo systemctl'
alias ls='ls --group-directories-first --color=auto'
alias ll='ls -lv'
alias la='ls -lva'
alias grep='grep --color=auto'
alias svim='sudo vim'
alias suspend='sc suspend'
alias clip='xclip -selection clipboard'
alias ws='ssh -p 244 paul@councilofcode.org'
alias gls='git status'
alias tree='tree -C'
alias pdf='evince'
alias nyan='cat'
alias record-desktop='ffmpeg -f x11grab -r 30 -s 1366x768 -i :0.0 -vcodec libx264 -threads 0'
alias wgetpaste='wgetpaste -C'
alias pastie='wgetpaste -C'
alias lg='sudo journalctl -b'
alias ntfs-mount='sudo mount -t ntfs -o rw,auto,uid=$UID,gid=$GID,fmask=0133,dmask=0022'

export PATH="/usr/lib/colorgcc/bin:"$HOME"/.android/sdk/platform-tools:"$HOME"/.android/sdk/tools:"$PATH":"$HOME"/.bin/:."
export ANDROID_HOME=$HOME"/.android/sdk/"
export EDITOR=vim
export WINE_DEBUG=-all
export BROWSER=chromium

eval `keychain --quiet --eval --agents ssh ~/.ssh/id_rsa`
